# ``HTMLKit/ForEach``

@Metadata {
    @DocumentationExtension(mergeBehavior: append)
}

```swift
ForEach(in: context.categories) { category in
    Division {
        Paragraph {
            category.title
        }
        ...
    }
}
```
